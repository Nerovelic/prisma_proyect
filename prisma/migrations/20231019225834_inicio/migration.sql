-- CreateTable
CREATE TABLE "Estudiante" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "nombre" TEXT NOT NULL,
    "genero" TEXT NOT NULL,
    "edad" INTEGER NOT NULL,
    "carrere" TEXT NOT NULL
);
